# Formation GaussSuppression
Formation à l'utilisation du package de protection de données tabulées par suppression `GaussSuppression`.

Astuce: faites tourner `renv::restore()` pour installer les packages nécessaires à la réalisation de ces TP.
